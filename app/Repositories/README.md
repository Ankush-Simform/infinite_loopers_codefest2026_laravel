# Repositories

Repositories are optional and should be introduced when query logic becomes reusable or complex.

Keep simple CRUD close to the service/model. Use repository classes for filtering, searching, sorting, and query composition that multiple services need.
