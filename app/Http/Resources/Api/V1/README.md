# API Resources

Place response transformers here.

Resources should expose mobile-friendly fields only. Avoid leaking internal storage paths, implementation flags, encryption details, or raw AI provider payloads.
